name = "greet"
description = "Greet a person by name"
command = "echo 'Hello, {name}! Welcome to the hot-reload demo.'"

[parameters_schema]
type = "object"
required = ["name"]

[parameters_schema.properties.name]
type = "string"
description = "The name of the person to greet"
